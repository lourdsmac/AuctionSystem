# Optional: absolute API base (no trailing slash). If unset, the Vite dev server proxies /api and /ws to the backend.
# VITE_API_BASE=http://localhost:5088

# Optional: override proxy target when VITE_API_BASE is unset (default http://localhost:5088)
# VITE_DEV_PROXY_TARGET=http://localhost:5088
